# Local dev: point at the FastAPI server (use 8010 if 8000 is busy).
VITE_API_BASE_URL=http://localhost:8000

# Production (Vercel): leave unset. vercel.json proxies /health, /rules,
# /gateway, /logs, and /stats to Render; the app uses same-origin requests.